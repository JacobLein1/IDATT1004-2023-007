final adjustUrl = Uri(
  scheme: scheme,
  host: baseUrl,
  port: port,
  path: "adjust",
);

final fireUrl = Uri(
  scheme: scheme,
  host: baseUrl,
  port: port,
  path: "fire",
);

const baseUrl = "192.168.0.0";
const int port = 7878;
const scheme = "http";
